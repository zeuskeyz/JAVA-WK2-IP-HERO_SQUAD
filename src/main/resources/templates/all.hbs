<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.7.0.slim.min.js" integrity="sha256-tG5mcZUtJsZvyKAxYLVXrmjKBVLd6VpVccqz/r4ypFE=" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.4/css/jquery.dataTables.css" />
    <script src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.js"></script>
</head>

<style>
    *{
        margin: 0;
        padding: 0;
    }
    #tables-page {
        background-image: url("images/shadows.jpg");
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
        height: 100vh;
    }
</style>
<body class="text-danger" id="tables-page">

    <div class="container-fluid" >
        <div class="row my-3 py-3">
            <div class="col-7">
                {{#if heroObject}}
                <legend class="text-center text-light">LIST OF HEROES</legend>
                <table class="table table-hover table-dark table-bordered border-secondary py-1" id="myTable">
                    <thead>
                        <tr>
                            <th scope="col">HERO</th>
                            <th scope="col" class="text-center">AGE</th>
                            <th scope="col">POWER</th>
                            <th scope="col" class="text-center">SCORE<br></th>
                            <th scope="col">WEAKNESS</th>
                            <th scope="col">MEMBER SQUAD</th>
                        </tr>
                    </thead>

                    <tbody>
                        {{#each heroObject}}
                        <tr>
                            <td>{{hero}}</td>
                            <td class="text-center">{{age}}</td>
                            <td>{{power}}</td>
                            <td class="text-center">{{power_score}}</td>
                            <td>{{weakness}}</td>
                            <td>{{squad}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                {{else}}
                    <div class="text-light d-flex justify-content-center align-items-center my-5 py-5">  <h1 class="text-center my-5 py-5 lead">You currently have no Heroes.<br>Consider adding some using the button on your left below!</h1></div>
                {{/if}}
            </div>

            <div class="col-5">
                {{#if squadObject}}
                    <legend class="text-center text-light">SQUADS LIST</legend>
                    <table class="table table-hover table-dark table-bordered border-secondary py-1" id="squadTable">
                        <thead>
                            <tr>
                                <th scope="col">SQUAD</th>
                                <th scope="col">CAUSE</th>
                                <th scope="col" class="text-center">MAX_SIZE</th>
                            </tr>
                        </thead>

                        <tbody>
                        {{#each squadObject}}
                            <tr>
                                <td>{{squad}}</td>
                                <td>{{cause}}</td>
                                <td class="text-center">{{size}}</td>
                            </tr>
                        {{/each}}
                        </tbody>
                    </table>
                {{else}}
                    <div class="text-light d-flex justify-content-center align-items-center my-3">  <h4 class="text-center my-5 py-5 lead">You currently have no Squads.<br>Consider adding some using the button on your left below!</h4></div>
                {{/if}}
            </div>

        </div>
    </div>
    <div class="text-center "> <a href="/home" type="button" class="btn btn-link text-decoration-none text-light">Back Home</a> </div>

    <script>
        $(document).ready( function () {

            $('#myTable').DataTable({
                pageLength : 5,
                lengthMenu: [[5, 10, 25, -1], [5, 10, 25,50]]
            });

            $('#squadTable').DataTable({
                pageLength : 5,
                lengthMenu: [[5, 10, 25, -1], [5, 10, 25,50]]
            });

        });
    </script>

</body>
</html>